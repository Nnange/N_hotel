server:
  port: 9192

spring:
  datasource:
    username: root
    password: admin
    url: jdbc:mysql://localhost:3306/n_hotel_db
    driver-class-name: com.mysql.cj.jdbc.Driver
  profiles:
    active: local
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format_sql: true
    servlet:
      multipart:
      max-file-size: 5MB
      max-request-size: 5MB
      file-size-threshold: 20KB